{
  "name": "user-product-portal",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node backend/src/app.js",
    "build:frontend": "cd frontend && npm install && ng build --configuration production",
    "start:dev": "nodemon backend/src/app.js",
    "deploy": "npm i && npm run build:frontend && npm run start"
  },
  "dependencies": {
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22",
    "typescript": "^5.0.4"
  }
}
